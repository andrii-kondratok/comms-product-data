"""Щоденний конвеєр Comms Product.

Кожна задача — функция `run(ctx) -> dict`, яку запускає `pipeline.runner`:
він бере блокування, пише ops.run, оновлює ops.job і водяні знаки.
Розклад — у таблиці ops.job, а не в коді: вимкнути задачу = один UPDATE.
"""
